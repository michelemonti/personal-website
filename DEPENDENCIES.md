# External dependency

The website has one external dependency:

- [Three.js 0.154.0](https://threejs.org/) — loaded from jsDelivr on the home
  and “work with me” pages for WebGL rendering.

Everything else is local, hand-written HTML, CSS and JavaScript. The site uses
the system font stack and includes no framework, UI library, analytics SDK,
package manager or remote font service.
